const caching = require("./caching");
console.log(caching.getName());
caching.setName("Superman");
console.log(caching.getName());


const newSuperHero = require("./caching");
console.log(newSuperHero.getName());
